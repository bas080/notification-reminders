<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".NotificationRemindersApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotificationReminders">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CrashReportActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".CreateReminderActivity"
            android:exported="true"
            android:label="@string/add_reminder"
            android:theme="@style/Theme.NotificationReminders.Dialog">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_REMINDER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PickNotificationActivity"
            android:exported="false"
            android:theme="@style/Theme.NotificationReminders.Dialog">
        </activity>

        <activity
            android:name=".SnoozeDialogActivity"
            android:exported="false"
            android:theme="@style/Theme.NotificationReminders.Dialog">
        </activity>

        <activity
            android:name=".ProcessTextActivity"
            android:exported="true"
            android:label="@string/add_reminder"
            android:theme="@style/Theme.NotificationReminders.Dialog">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <provider
            android:name=".providers.RemindersContentProvider"
            android:authorities="com.bas080.notificationreminders.provider"
            android:exported="true" />

        <receiver
            android:name=".receivers.CreateReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.bas080.notificationreminders.ACTION_CREATE_REMINDER" />
                <action android:name="com.bas080.notificationreminders.ACTION_DONE_REMINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.ReminderNotificationListenerService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Notification monitoring for user reminders" />
        </service>

    </application>

</manifest>
